// import React from 'react';

const IconRegistry = () => {
    //     const requireSvg = import.meta.glob('./assets/icons/*.svg', { eager: true });
    //     console.log("required keys:", requireSvg);

    //     const iconElements = requireSvg.keys().map((fileName) => {
    //         const id = fileName.replace('../', '').replace('.svg', '');

    //         // 获取 SVG 文件的内容
    //         const svgContent = requireSvg(fileName);

    //         return (
    //             <symbol id={`icon-${id}`} key={id} viewBox="0 0 1024 1024">
    //                 {/* 使用 dangerouslySetInnerHTML 插入 SVG 内容 */}
    //                 <g dangerouslySetInnerHTML={{ __html: svgContent }} />
    //             </symbol>
    //         );
    //     });

    //     return <svg style={{ display: 'none' }}>{iconElements}</svg>;
};

export default IconRegistry;
